Public Class ConfirmationForm
    Public Sub New(txID As String)
        IDLabel.Text = txID
        ' This call is required by the designer.
        InitializeComponent()

        ' Add any initialization after the InitializeComponent() call.

    End Sub
End Class